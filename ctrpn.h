#pragma once
#include <iostream>
#include <stack>
#include <cctype>
#include <map>
#include <algorithm>
#include <string>

void toNoramlForm(std::string& sourceString);
std::string RPN(std::string str);
